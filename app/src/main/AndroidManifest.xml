<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.com.uco.carmeloadmin">

    <application
        android:name=".util.GlobalState"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.RegistroUsuarioActivity" />
        <activity android:name=".view.InicioActivity" />
        <activity android:name=".view.DespieceActivity" />
        <activity android:name=".view.NuevaPuertaActivity" />
        <activity
            android:name=".view.PresentacionActivity"
            android:label="@string/title_activity_presentacion"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>